这是一个非常经典的前端性能挑战。当 React 消息流内容变多（尤其是包含复杂的 Markdown、表格、多段 SQL 高亮）时，卡顿的核心原因是：**每次字符追加都会触发整个消息组件（甚至整个列表）的深度重新渲染（Re-render）。**

以下是解决该问题的 4 个核心思路：

### 1. 最小化渲染范围 (Component Memoization)
**问题：** 新字符进入时，之前已经渲染好的消息、侧边栏、历史记录都在跟着重绘。
**方案：** * 使用 `React.memo` 包裹历史消息组件。只有当前正在更新的那条消息（`isStreaming: true`）才允许执行渲染逻辑。
* 将“正在打字”的状态局部化。不要在 `App.tsx` 的顶级 State 里存所有的文本，而是只在当前消息组件内部处理流。

### 2. 虚拟化长列表 (Windowing/Virtualization)
**问题：** 随着对话增加，DOM 节点指数级增长。渲染一个包含 50 条带 SQL 高亮消息的页面，浏览器压力极大。
**方案：** * 使用 **`react-virtuoso`** 或 **`react-window`**。
* 只渲染可视区域内的消息。对于不可见的消息，直接卸载或用简单的占位符代替，这样 React 只需要计算当前屏幕内那几条消息的 Diff。

### 3. 规避复杂的正则与高亮重算 (Incremental Parsing)
**问题：** SQL 代码高亮（如 `Prism` 或 `Shiki`）非常耗 CPU。如果每出一个字符就重新对全段 SQL 进行语法分析，必定卡顿。
**方案：** * **截断渲染：** 在消息处于 `isStreaming` 状态时，降低语法高亮的频率（例如每增加 5-10 个字符才重绘一次高亮，或者打字期间只用普通文本，打完后再高亮）。
* **CSS 优化：** 代码块开启 `contain: content;` 或 `content-visibility: auto;`，告诉浏览器这一块的布局变化不影响外部。

### 4. 这里的渲染“核武器”：避免 React 处理流 (Ref / DOM Bypass)
**思路：** React 的 `useState` 配合 `Markdown` 解析器是极其昂贵的。
**方案：** * **非受控更新：** 使用 `useRef` 直接引用 DOM 节点。当流式数据到达时，直接通过 `ref.current.innerText += chunk` 更新。
* **分段解析：** 只有当检测到特定的结束标记（如 `\n` 或 ```）时，才触发一次 React 的 State 更新来渲染最终的 Markdown 结构。
* **双 Buffer 机制：** 前端维护一个缓冲区。每秒只更新 10-15 次 UI，而不是每次 WebSocket/EventSource 有数据就触发 `setState`（人类肉眼其实分辨不出每秒 60 次和 15 次打字的差别）。

---

### 给 Claude 的补充优化建议：
你可以让 Claude 在写 `useStreaming` Hook 时加入**节流（Throttling）**：

> “Claude，在实现流式输出时，请对 `setState` 进行节流处理，每 30ms-50ms 合并更新一次内容，避免由于字符追加频率过快导致的渲染阻塞。”

**这样你的 Demo 即使生成几千字的 SQL，依然能像丝滑的奶油一样流畅。**

为了解决流式输出（Streaming）在 React 中导致的卡顿，我们不能简单地每收一个字符就 `setState`。核心思路是：**使用「双缓存」机制（Buffer）并结合「节流更新」（Throttled Update）**。

这份代码展示了如何编写一个高性能的 `useStreaming` Hook，它能确保即使大模型在疯狂吐字，你的 UI 依然丝滑。

### 核心实现：`useStreaming.ts`

```typescript
import { useState, useRef, useEffect } from 'react';

export const useStreaming = (onFinished?: () => void) => {
  const [displayContent, setDisplayContent] = useState("");
  const [isStreaming, setIsStreaming] = useState(false);
  
  // 核心：使用 Ref 作为缓冲区，避免 Ref 变化触发 Re-render
  const bufferRef = useRef("");
  const timerRef = useRef<NodeJS.Timeout | null>(null);

  const startStream = async (fullText: string) => {
    setIsStreaming(true);
    setDisplayContent("");
    bufferRef.current = "";
    
    let index = 0;
    const chunkSize = 3; // 每次追加的字符数，增加此值可加速感官速度
    const interval = 30; // 节流频率（毫秒），30ms 是肉眼舒适的极限

    return new Promise<void>((resolve) => {
      timerRef.current = setInterval(() => {
        if (index < fullText.length) {
          // 1. 将新字符压入缓冲区
          const nextChunk = fullText.slice(index, index + chunkSize);
          bufferRef.current += nextChunk;
          index += chunkSize;

          // 2. 节流更新：只在此处触发一次 React 渲染
          // 这样无论 fullText 有多长，React 的渲染频率都是恒定的
          setDisplayContent(bufferRef.current);
        } else {
          // 结束流
          if (timerRef.current) clearInterval(timerRef.current);
          setIsStreaming(false);
          onFinished?.();
          resolve();
        }
      }, interval);
    });
  };

  // 这里的清理逻辑很重要，防止组件销毁后定时器还在跑
  useEffect(() => {
    return () => {
      if (timerRef.current) clearInterval(timerRef.current);
    };
  }, []);

  return { displayContent, isStreaming, startStream };
};
```

---

### 为什么这样写就不卡了？

1.  **恒定渲染频率 (Fixed Frame Rate)**: 
    传统的写法是 `forEach` 字符然后 `await sleep`。如果字符来得极快，React 会短时间内堆积成百上千个渲染任务。这个 Hook 强制将渲染频率锁定在约 **33FPS**（30ms 一次），这符合显示器刷新逻辑，极大减轻了渲染压力。
    
2.  **块处理 (Chunking)**: 
    通过 `chunkSize` 控制每次更新的步长。对于 SQL 代码块这种重负载组件，一次塞入 5 个字符比一个一个塞入要节省 80% 的解析开销。

3.  **Ref 隔离**: 
    所有的字符串拼接逻辑都在 `bufferRef` 中完成，React 不需要追踪字符串拼接的中间状态，只需要在定时器触发时拿走最终结果。

---

### 如何在 `MessageRenderer` 中进一步优化？

即使数据更新慢了，如果 `react-markdown` 每次解析太重怎么办？你可以给渲染器加一个简单的 **`memo`**。

```tsx
import { memo } from 'react';

const MemoizedSQL = memo(({ code }: { code: string }) => {
  return (
    <SyntaxHighlighter language="sql" style={oneLight}>
      {code}
    </SyntaxHighlighter>
  );
}, (prev, next) => {
  // 只有当代码块内容长度变化超过一定阈值，或者流结束时才重新渲染高亮
  // 这是一个极端的优化技巧：每 20 个字符才重新计算一次高亮
  return Math.abs(next.code.length - prev.code.length) < 20 && next.code.endsWith === prev.code.endsWith;
});
```



### 给 Claude 的最后建议：
你可以把这个 Hook 交给 Claude，让它在 `App.tsx` 中调用。这样当你点击“演示”时，你会发现复杂的表格和 SQL 能够非常从容地、一行一行地“长”出来，而不是卡顿着跳出来。

**需要我帮你把这个 Hook 整合进 `App.tsx` 的完整逻辑中吗？**